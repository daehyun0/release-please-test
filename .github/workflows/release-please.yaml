on:
  push:
    branches:
      - release4
name: release-please
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: node
          default-branch: release4
          versioning-strategy: always-bump-patch